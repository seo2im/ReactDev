import React from 'react';
import ReacDOM from 'react-dom';

const App: React.FC = () => {
    return (
        <div>
            TEST
        </div>
    );
};

ReacDOM.render(<App />, document.getElementById('root'));
